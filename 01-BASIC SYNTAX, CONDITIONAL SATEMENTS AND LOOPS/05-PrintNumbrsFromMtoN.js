
function printNumsFromNToM(bigNumber, littleNumber) {
   while (bigNumber >= littleNumber) {
       console.log(bigNumber);
       bigNumber--; 
   } 
}

// printNumsFromNToM (6,2);
printNumsFromNToM (4,1);